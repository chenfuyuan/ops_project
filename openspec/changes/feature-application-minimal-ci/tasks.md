## 1. 规格与文档联动

- [x] 1.1 更新 `docs/cicd_baseline.md`，明确主基线与“应用代码最小 CI 实践”之间的职责分层
- [x] 1.2 补充或新增面向应用代码最小 CI 实践的说明文档，使其覆盖默认范围、三层模型与阻断语义
- [x] 1.3 校对相关基线文档中的术语与边界，确保不将文档门禁、CD 或平台配置误写入本次范围

## 2. 静态与结构校验层

- [x] 2.1 盘点当前仓库已有的格式、静态检查、依赖/安全最低检查与架构/边界检查资产
- [x] 2.2 收敛出应用代码最小 CI 的默认静态与结构校验集合，并明确哪些检查属于默认阻断项
- [x] 2.3 将架构/边界检查对齐到 `app/` 与相关测试范围，确保其可作为默认阻断门禁稳定执行
- [x] 2.4 基于 `uv` 建立或整理静态与结构校验层的最小执行命令入口

## 3. 测试校验层

- [x] 3.1 盘点 `tests/unit/`、`tests/integration/`、`tests/architecture/` 中现有可纳入默认准入的测试集合
- [x] 3.2 明确应用代码最小 CI 默认测试集合，限定为单元测试、适当的集成测试与架构测试
- [x] 3.3 为默认测试集合补齐缺失的最小测试或调整现有测试，使其与应用代码最小 CI 范围一致
- [x] 3.4 基于 `uv` 建立或整理测试校验层的最小执行命令入口

## 4. 可运行性与构建确认层

- [x] 4.1 确认可运行性/构建确认层的最小验证方式，例如导入校验、装配校验或等价运行时确认
- [x] 4.2 落实应用代码最小 CI 第三层所需的最小校验资产，确保其不扩展为制品发布或 CD 流程
- [x] 4.3 基于 `uv` 建立或整理可运行性/构建确认层的最小执行命令入口
- [x] 4.4 组合三层最小 CI 的默认执行顺序，并验证其可以作为后续平台无关流水线映射基础
