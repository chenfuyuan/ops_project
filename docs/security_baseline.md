# 安全基线

## 文档目标
本文档定义项目的最低安全要求，覆盖应用安全、数据安全、配置安全、依赖安全与安全例外管理。

## 适用范围
- API 与 Worker 应用层
- 配置与密钥管理
- 数据库存储与对象存储访问
- 构建、依赖与发布变更过程

## 本文负责 / 不负责
### 本文负责
- 定义统一安全原则
- 定义应用层、数据层和依赖层的最低安全要求
- 定义日志脱敏与审计约束
- 定义安全例外管理要求

### 本文不负责
- 不替代具体的鉴权业务设计文档
- 不展开密钥注入流程，详见 `docs/configuration_and_secrets_management.md`
- 不展开发布步骤，详见 `docs/release_change_and_rollback.md`

## 统一安全原则
- 最小权限
- 默认拒绝
- 敏感信息不入库、不入日志、不入业务核心
- 边界清晰，技术细节不回流到业务核心
- 高风险变更必须可审查、可追踪、可回滚

## 应用层安全要求
- 所有外部输入都应在系统边界进行校验
- 鉴权和授权逻辑应明确位于协议边界或业务明确约定的位置
- 不应在业务核心中散落处理原始认证令牌或底层权限实现细节
- 远程调用鉴权、签名、超时和重试应留在基础设施适配层

## 数据与存储安全
- PostgreSQL 承载核心业务数据和可审计元数据
- Redis 不应作为唯一业务事实来源保存关键安全状态
- 对象存储中的文件访问应受控，避免公开暴露默认可读对象
- 涉及敏感字段时，应优先保存必要最小信息

## 配置与密钥安全
- 密钥和凭据不得硬编码
- 不同环境必须隔离密钥
- 调试信息不得输出敏感配置原文
- 在容器或 Compose 场景下，敏感配置必须通过运行时环境变量或等价外部注入机制进入容器，不能写入镜像层或仓库
- 详见 `docs/configuration_and_secrets_management.md`

## 容器与镜像安全
- 容器化只是运行形态，不改变业务边界、配置边界和敏感信息处理边界
- 镜像中不得硬编码密钥、令牌、数据库凭据或其他敏感配置
- 镜像内运行代码所依赖的 Python 依赖来源仍应以 `pyproject.toml` 和 `uv.lock` 为单一事实来源
- 若仓库当前只提供最小 Docker 基线，不应在安全评审中假定镜像扫描、镜像签名、制品仓库准入或平台侧策略已经落地
- Python 依赖以 `pyproject.toml` 和 `uv.lock` 为单一事实来源
- 依赖升级应受评审控制
- 不引入来源不明或维护状态不清晰的核心依赖
- 遇到安全漏洞时，应优先评估影响范围、缓解措施和升级计划

## 流水线与制品安全
- 依赖/安全最低门禁属于默认 CI 门禁的一部分，并作为最小交付准入条件处理
- 进入受控部署前，应确保可发布制品可追踪、可验证，并能与发布记录和回滚入口关联
- 当仓库尚未落地镜像扫描、制品签名、审批链或其他企业平台能力时，不得在安全基线中将其写成现成控制
- 流水线侧安全控制应与 `docs/cicd_baseline.md` 的平台中立阶段模型协同，并明确不绑定特定平台语法或供应商能力

## 日志与审计安全
- 日志必须对密钥、凭据、令牌和敏感连接信息做脱敏
- 关键变更应可追踪到责任人和时间点
- 高风险操作应保留足够的上下文用于事后审查

## 安全例外管理
当出现无法立即满足基线的情况时，应记录：
- 例外原因
- 影响范围
- 临时补偿措施
- 责任人
- 失效时间或清理时间

## 角色与责任
- 开发：保证新增代码遵守输入校验、敏感信息保护和依赖控制原则
- 评审者：检查越权访问、敏感信息暴露和边界破坏风险
- 发布责任人：确保高风险变更有审查和回滚准备

## 与其他文档关系
- 环境与部署：`docs/deployment_environment_baseline.md`
- 配置与密钥：`docs/configuration_and_secrets_management.md`
- 发布与回滚：`docs/release_change_and_rollback.md`
- 架构边界与依赖控制：`docs/architecture_baseline/03-anti-corruption-and-dependencies.md`

## 校验要点
- 是否在系统边界处理外部输入校验
- 是否避免在业务核心中处理底层安全实现细节
- 是否对日志和配置进行了脱敏要求
- 是否对依赖和高风险变更设置了最小控制要求
- 在容器化场景下，是否明确了镜像层不得承载密钥与敏感配置
- 是否避免把尚未存在的镜像扫描、签名或制品治理能力写成现成安全控制

## 例外与变更机制
如因第三方限制、遗留系统约束或短期交付压力需要暂时突破安全基线，必须形成明确记录，并在例外结束后回收。